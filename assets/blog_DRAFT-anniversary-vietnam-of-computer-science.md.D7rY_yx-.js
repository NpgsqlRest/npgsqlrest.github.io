import{_ as t,c as a,o as i,a5 as s}from"./chunks/framework.CgT1UzWm.js";const n="/ddd-agggrate-transparent.webp",o="/vietnam/evans.png",r="/vietnam/vernon.png",l="/vietnam/codd.png",b=JSON.parse('{"title":"DRAFT: 20th Anniversary of The Vietnam of Computer Science","titleTemplate":"NpgsqlRest","description":"DRAFT — A compilation of a decade of arguments about DDD, Clean Architecture, the wrong abstractions modern business software is built on, and the case for putting the database back where it belongs.","frontmatter":{"layout":"doc","outline":[2,3],"title":"DRAFT: 20th Anniversary of The Vietnam of Computer Science","titleTemplate":"NpgsqlRest","description":"DRAFT — A compilation of a decade of arguments about DDD, Clean Architecture, the wrong abstractions modern business software is built on, and the case for putting the database back where it belongs.","badge":"human","head":[["meta",{"name":"robots","content":"noindex, nofollow"}],["meta",{"name":"keywords","content":"npgsqlrest postgresql clean architecture ddd database-first state abstraction sql platform business rules"}]]},"headers":[],"relativePath":"blog/DRAFT-anniversary-vietnam-of-computer-science.md","filePath":"blog/DRAFT-anniversary-vietnam-of-computer-science.md"}'),h={name:"blog/DRAFT-anniversary-vietnam-of-computer-science.md"};function d(c,e,p,m,u,g){return i(),a("div",null,e[0]||(e[0]=[s('<h1 id="draft-20th-anniversary-of-the-vietnam-of-computer-science" tabindex="-1">DRAFT: 20th Anniversary of The Vietnam of Computer Science <a class="header-anchor" href="#draft-20th-anniversary-of-the-vietnam-of-computer-science" aria-label="Permalink to &quot;DRAFT: 20th Anniversary of The Vietnam of Computer Science&quot;">​</a></h1><p class="blog-meta"><span>DRAFT — TODO: date</span> · <span class="tag">NpgsqlRest</span><span class="tag">PostgreSQL</span><span class="tag">Architecture</span><span class="tag">DDD</span><span class="tag">Clean Architecture</span><span class="tag">Opinion</span></p><p><img src="https://img.shields.io/badge/Human-Written-blue" alt=""><img src="https://img.shields.io/badge/DRAFT-orange" alt=""></p><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>It has been twenty years since Ted Neward published <a href="https://www.odbms.org/wp-content/uploads/2013/11/031.01-Neward-The-Vietnam-of-Computer-Science-June-2006.pdf" target="_blank" rel="noreferrer">&quot;The Vietnam of Computer Science&quot;</a>. Twenty years.</p><p>Did we win? Did we even leave? Are we stuck in a quagmire?</p><p>Since then, the industry has marched relentlessly through a never-ending parade of patterns, architectures, and methodologies: ORM tools of every flavor, Repository and Unit of Work patterns, Domain-Driven Design, CQRS, Event Sourcing (currently all the rage), Hexagonal Architecture, Clean Architecture, Onion Architecture, Service-Oriented Architecture (now obsolete and so last year), Microservices, and on and on.</p><p>The quagmire Neward described is, of course, Object-Relational Impedance Mismatch. So to speak, a shotgun marriage between Object and Relational worlds. One lives in your application memory and works over data structures, and the other, well, in a relational database.</p><p>And none of these patterns above made the problem go away. We need both, and somehow the majority of the effort goes into dealing with persistence in one way or another. It&#39;s like unsuccessful couples therapy for that shotgun marriage. Maybe we need divorce papers?</p><p>Let&#39;s dig in deep.</p><h2 id="what-is-the-object–relational-impedance-mismatch" tabindex="-1">What Is The Object–Relational Impedance Mismatch <a class="header-anchor" href="#what-is-the-object–relational-impedance-mismatch" aria-label="Permalink to &quot;What Is The Object–Relational Impedance Mismatch&quot;">​</a></h2><p>According to Wikipedia (link: <a href="https://en.wikipedia.org/wiki/Object%E2%80%93relational_impedance_mismatch" target="_blank" rel="noreferrer">https://en.wikipedia.org/wiki/Object–relational_impedance_mismatch</a>):</p><blockquote><p>Object–relational impedance mismatch is a <strong>set of difficulties</strong> going between data in relational data stores and data in domain-driven object models.</p></blockquote><p>Ok, got it. A set of difficulties. Difficulties that refuse to go away, it seems, but fine.</p><p>It&#39;s worth noting that Object–Relational isn&#39;t the only mismatch on the menu. OO isn&#39;t the only way we work over application memory — functional programming is on the rise too, so there&#39;s a Functional–Relational mismatch as well. To be fair, FP gets along with relational better than OO does: SQL is already declarative, set-based, and value-oriented, right up FP&#39;s alley. But it still works over application memory, so it still has to bridge to the relational database like everyone else.</p><p>Anyway...</p><blockquote><p>Relational Database Management Systems (RDBMS) is the standard method for storing data in a dedicated database, while object-oriented (OO) programming is the default method for business-centric design in programming languages.</p></blockquote><p>RDBMS is the standard method for storing data in a dedicated database, but are they typically doing only data storage? I mean, files are also doing that, are they not? Are we missing something here?</p><p>And this second claim that object-oriented (OO) programming is the default method for business-centric design, that might be true, but SQL is the default method for anything business data-related, like the way organizations store, manage, protect, and analyze their data.</p><p>We can see the tension already here, but let&#39;s move on.</p><blockquote><p>The problem lies in neither relational databases nor OO programming, but in the conceptual difficulty mapping between the <strong>two logic models</strong>. <strong>Both logical models</strong> are differently implementable using database servers, programming languages, design patterns, or other technologies.</p></blockquote><p>Now, why would they say that we have two logical models?</p><p>Take a Customer. In your business, a customer is one thing — one concept. DDD even has a name for this: within a bounded context there is one ubiquitous language, so there is one shared notion of what a Customer is. That single concept becomes one logical model — what a Customer is, which attributes it has, how it relates to orders — with no implementation details attached. That&#39;s why it&#39;s called a <em>logical</em> model.</p><p>Then you build it. And here is where it splits: that one logical model gets implemented physically more than once. Once as a table — <code>customer</code>, with a <code>varchar(255)</code> name column. Once as a class — <code>Customer</code>, with a <code>string Name</code> property. Same concept, same logical model, two physical incarnations.</p><p>So in modern software design we are not talking about two different logical models, as Wikipedia claims. We have one logical model and two — sometimes more — physical models. Modern overengineering knows no bounds.</p><p>The reason why they say that we have two logical models is probably because OO and Relational are seen as totally different paradigms so the logical models must be genuinely different. For example OO does have behavior, but the problem is that we are talking strictly about data here, not behavior. Behavior is a different problem axis - we&#39;ll come to it when we get to algorithms.</p><p>In any case, impedance mismatch is not about two different logical models, but about the mismatch between those two physical models. Your system has to map them and make them work together because, logically, we still have one logical model and the system needs to reflect that. That’s the real problem and source of tension.</p><blockquote><p>Issues range from application to enterprise scale, whenever stored relational data is used in domain-driven object models, and vice versa. Object-oriented data stores can trade this problem for other implementation difficulties.</p></blockquote><p>This last sentence just confirms what I just said. Whenever stored relational data (one physical model) is used in domain-driven object models (another physical model), we have this problem or mismatch. Maybe the belief that we are talking about two different logical models is the reason why these difficulties exist and persist and are not being resolved. If it was just a simple mapping, let&#39;s say from <code>varchar(255)</code> to a <code>string</code>, that would be easy to solve a long time ago. Or something a bit more complex, let&#39;s say many-to-many on a logical model level. In a relational model, that requires a junction table, but in an object model that can be just a collection of references. Still, a little bit more complex mapping, but still solvable and indeed already solved.</p><p>My sincere belief is that we are talking here about a series of misconceptions and misunderstandings about the nature of abstractions themselves. More specifically, about abstractions the RDBMS already provides. And if RDBMS already provides them, well, that means that the application layer goes on and re-implements them anyway. No wonder we have difficulties. Let&#39;s look at these misconceptions one by one and discuss in detail:</p><ol><li><p><strong>State Data</strong> Abstraction Misconception</p></li><li><p><strong>Storage Devices</strong> Abstraction Misconception</p></li><li><p><strong>Data Structures</strong> Abstraction Misconception</p></li><li><p>Abstraction Over <strong>Algorithms</strong></p></li><li><p>Abstraction Over <strong>Concurrency and Integrity</strong></p></li></ol><h2 id="_1-state-data-abstraction-misconception" tabindex="-1">1) State Data Abstraction Misconception <a class="header-anchor" href="#_1-state-data-abstraction-misconception" aria-label="Permalink to &quot;1) State Data Abstraction Misconception&quot;">​</a></h2><h3 id="the-claim" tabindex="-1">The Claim <a class="header-anchor" href="#the-claim" aria-label="Permalink to &quot;The Claim&quot;">​</a></h3><p>Object-oriented programming has one of its core tenets called <strong>encapsulation</strong>. Encapsulation is supposed to protect internal data — <strong>the state</strong>. The object is the authoritative custodian of its state. Nobody else has it. Period. Without encapsulation, we don&#39;t really have OOP anymore.</p><p>On the other hand, functional programming has its own version of that — <strong>state immutability</strong>. A function takes values in and returns new values out, leaving the originals untouched — so there&#39;s no shared mutable state to corrupt in the first place. FP also enforces valid state through invariants, often by encoding them directly into the type system. Same goal, fewer bugs from uncontrolled state, arguably reached more elegantly. Fine. State status - protected, bugs - reduced. Great, beautiful, love it.</p><p>This, in fact, is very reasonable and well-thought-out. State data is data shared between different parts of the system and even different users. If every part of the system can poke at it without other parts knowing about it, then you have a lot of bugs. So, naturally, over time people came up with these concepts of encapsulation and immutability to protect that state and make sure it is only changed and fiddled with in a controlled way. Because we don&#39;t want to have bugs. Bugs are bad, okay.</p><p>No objection here. This is perfectly fine, and it makes a lot of sense. Let&#39;s say we have something complex. A game scene. Compiler syntax tree. Whatever. Protecting state in memory of such systems is invaluable, to say at least.</p><p>In his book Domain-Driven Design (2004), when describing the Domain Layer in Chapter 4, &quot;Isolating the Domain,&quot; Eric Evans writes:</p><blockquote><p>State that reflects the business situation is controlled and used here, even though the technical details of storing it are delegated to the infrastructure.</p></blockquote><p>So he treats the in-memory object as the custodian of state - state that is &quot;controlled and used here,&quot; where &quot;here&quot; is the in-memory Domain Layer — while the RDBMS is merely &quot;delegated to the infrastructure.&quot; More on that in the next chapter. What matters here is the claim itself: the state is in memory. No doubt about it.</p><h3 id="the-reality" tabindex="-1">The Reality <a class="header-anchor" href="#the-reality" aria-label="Permalink to &quot;The Reality&quot;">​</a></h3><p>But what about applications backed by relational databases, business or otherwise?</p><p>In RDBMS-backed applications, the state lives in that RDBMS itself - not in memory. The authoritative custodian of state is the <strong>database table row</strong>, not the in-memory object.</p><p>Take any business application backed by a relational database. How do you check the current state of some entity? Do you peek at the object in memory? No, you query the database for that. Anyone who has worked 5 seconds in industry knows this, of course.</p><p>I know what DDD people will say now: object memory (or functional state memory) is the real state data, and RDBMS is just where that data is persisted (presumably when the user clicks on a &quot;Save&quot; icon).</p><p>And, if you still believe that objects/memory are the real state and not RDBMS, then riddle me this:</p><p>What if we have multiple instances of the application running behind a load balancer? And then maybe some background work as well, and some other services too. Maybe reporting replica as well, so we have multiple processes accessing the same state data. Who is the sole custodian of that state data in that case? The in-memory object of one process, or the database row? The obvious answer is, of course, NONE of the in-memory copies. The best we can do is to have each process hold a copy, while the real state is in the RDBMS itself.</p><p>Some may say now, but databases have multiplicity too, right? We have multiple replicas, and they are all copies of the same data. And there are also multi-master setups as well with multiple writers for high availability scenarios. Yeah, but the big difference is that the database solves it, while in-memory objects don&#39;t even try to be honest. They just deny the reality. For multiple replicas, there is never any ambiguity about which one is the source of truth, since we are talking about read-only copies, and for multi-master setups we have either different consensus protocols that ensure a single source of truth or so-called eventual-consistency for the source of truth. In-memory objects have no such machinery, so they just pretend that they are the single custodian of the state data, which is, of course, a big, fat lie.</p><p>If the object is the custodian of state, what if we kill and restart the application? Oh my, the object&#39;s custodianship has evaporated.</p><p>Also, what, for example, if we have two concurrent user writers? One loads an invoice as &#39;pending&#39;, but another writer marks it &#39;paid&#39; while the first one holds it. Now the first one is lying about the state of that invoice. The database is right - it is the source of truth - the first one is stale. We will talk more about concurrency and integrity later - this is just to prove my point:</p><p>RDBMS is the authoritative custodian of state, not in-memory objects. The state lives in the database, not in memory.</p><h3 id="the-cost" tabindex="-1">The Cost <a class="header-anchor" href="#the-cost" aria-label="Permalink to &quot;The Cost&quot;">​</a></h3><p>Take a look at this example of a DDD-style domain model below:</p><figure style="margin:0;text-align:center;"><img src="'+n+`" alt="" style="display:block;width:50%;max-width:100%;height:auto;margin:0 auto;"><figcaption style="font-size:0.8em;color:#888;margin-top:0.5em;"> Source credit: <a href="https://www.reddit.com/r/DomainDrivenDesign/comments/1ttzr19/create_complex_and_deep_aggregate/">https://www.reddit.com/r/DomainDrivenDesign/comments/1ttzr19/create_complex_and_deep_aggregate/</a></figcaption></figure><p>This is a random example from Reddit, but it is indicative. Virtually every domain model is more or less like that - strip away the method, adjust types a bit, and it is basically an Entity-Relationship (ER) diagram, that&#39;s it. There is no structural difference between the domain model and the database model. It is the same logical model implemented twice.</p><p>And that method <code>isValid()</code> - the only behavior in the whole model - is just a data integrity check. Every rule it enforces, the database enforces too: <code>dateBegin &lt;= dateEnd</code> is a one-line <code>CHECK</code>, and the harder ones —- price periods that must not overlap, quantity ranges that must stay contiguous — are exactly what database constraints exist for (more on the how in the integrity misconception). Same logical model implemented twice, same integrity rules implemented twice.</p><p>Because modern software design orthodoxy refuses to acknowledge that the state lives in the RDBMS, it forces us to implement the same model at least twice - once in the database (relational model implementation), once in memory (object model implementation, mapped usually with an O/R tool or a library).</p><p>I say at least, because there are examples where we have even more. Some &quot;architects&quot; will consider that O/R mapped model a persistence model because it doesn&#39;t have any behavior, and then they will add a separate domain model on top of that, which is the one with the behavior. So we have three implementations of the same logical model - database model, persistence model, and domain model.</p><p>And then, since we now have at least two physical models, we also have two type systems and two sets of constraints to protect the state. And we must keep them in sync at all times, and we must maintain the correct mapping. To be fair, this is mostly automated in modern systems (not completely), but we still have to do it and it is still there, automated or not. And even automated, it is still a cost and still a source of bugs and still a source of complexity. And even that can&#39;t bridge the semantic gap between the two type systems fully. For example, in PostgreSQL we can have a <code>NOT NULL</code> constraint on a column, but the corresponding C# property will be a nullable string.</p><p>All because we believe that there is some important state data we need to protect with our objects. In the best case, there are just some transient and disposable chunks of data copies, and in any case there is a lot of plumbing to manage: connections, transactions, commands, queries, etc. You know, the actual infrastructure.</p><p>Personally, I see a lot of irony in this. Modern orthodoxy calls RDBMS infrastructure, just to end up with code bases implementing, well, actual infrastructure.</p><h2 id="_2-storage-devices-abstraction-misconception" tabindex="-1">2) Storage Devices Abstraction Misconception <a class="header-anchor" href="#_2-storage-devices-abstraction-misconception" aria-label="Permalink to &quot;2) Storage Devices Abstraction Misconception&quot;">​</a></h2><h3 id="the-claim-1" tabindex="-1">The Claim <a class="header-anchor" href="#the-claim-1" aria-label="Permalink to &quot;The Claim&quot;">​</a></h3><p>A modern software engineering approach makes the assumption that an RDBMS is a storage device. A device used to store data. Therefore, good engineering practice is to abstract that storage device.</p><p>We can see that in the quote from Eric Evans above, where he says that:</p><blockquote><p>... the technical details of storing it are delegated to the infrastructure.</p></blockquote><p>Evans doesn&#39;t dwell on this point much - he just delegates storage to the infrastructure and moves on. On the other hand, Robert C. Martin is much more vocal about it. In <em>Clean Architecture</em>, Martin dedicates an entire chapter to &quot;The Database Is a Detail&quot; argument. He is very blunt about it, and he repeats it several times, for example:</p><blockquote><p>It&#39;s just a mechanism we use to move the data back and forth between the surface of the disk and the RAM.</p></blockquote><p>Or this:</p><blockquote><p>The database is really nothing more than a big bucket of bits where we store our data on a long-term basis.</p></blockquote><p>This is not some fringe blog post. These are two of the most-cited and influential authors in the field - arriving at one and the <strong>same conclusion</strong> - just at different volumes.</p><p>Evans states it just once and quietly, like he doesn&#39;t want to talk about it too much, and then delegates it away (perhaps he&#39;d rather not have you examine it too closely, I don&#39;t know). Martin states it over and over, bluntly, and goes as far as to insist we should not even acknowledge that the disk exists.</p><p>Different approaches - identical claims: the <strong>database is a storage device</strong>. Storage is mechanical, it sits beneath the business — no different from a file system — and the architect&#39;s job is to wrap it up tight and forget it is there.</p><h3 id="the-reality-1" tabindex="-1">The Reality <a class="header-anchor" href="#the-reality-1" aria-label="Permalink to &quot;The Reality&quot;">​</a></h3><p>Reality is that RDBMS uses a storage device or devices, and that means that it already does this abstraction for you. It already abstracts storage.</p><p>If we go back to the beginning, when Edgar Codd introduced the relational model in 1970, the pitch was <em>data independence</em> — the whole point was to insulate the logical shape of your data from how it physically sits on a device. In fact, that is the core concept of the relational model. It was codified as the numbered Rule 8 fifteen years later in <a href="https://en.wikipedia.org/wiki/Codd%27s_12_rules" target="_blank" rel="noreferrer">Edgar Codd&#39;s twelve rules</a>: <strong>Rule 8: Physical data independence</strong>:</p><blockquote><p>Application programs and terminal activities remain logically unimpaired whenever any changes are made in either storage representations or access methods.</p></blockquote><p>The ANSI/SPARC Architecture from that era formalized it and walled off internal storage as well. So, the relational model was designed, from day one, to hide the disk.</p><p>Back to modern times and modern RDBMS implementations.</p><p>Riddle me this: We can write a <code>SELECT</code>, a <code>JOIN</code>, a <code>WHERE</code>, and even an <code>INSERT</code>, <code>UPDATE</code>, or <code>DELETE</code>, and in most cases it will be executed with the same results on different RDBMS engines from different vendors. That is called ANSI/ISO standardized SQL, which goes to show the real separation between the model and the physical implementation. Virtually every RDBMS implements the same ANSI/ISO SQL standard, but with dialect differences at the edges. Those differences can sometimes be significant, but the core of the language is the same, and that is what matters here. The same <code>SELECT</code> statement can run on MySQL, PostgreSQL, SQL Server, Oracle, and so on, given that you have the same logical model implemented in each of them.</p><p>But the abstraction doesn&#39;t stop at the SQL language. The relational model and the RDBMS implementations built on top of it are designed to be agnostic to the underlying storage device. For example:</p><ul><li><p>In PostgreSQL, we can move any table to any <strong>different storage device</strong> we choose (they call it a tablespace), and the model above remains unchanged. Just use <code>ALTER TABLE ... SET TABLESPACE</code> — the table is now on a different physical device. The term <code>TABLESPACE</code> is Oracle&#39;s term for the same thing (switching table storage), and SQL Server has something called <code>FILEGROUPS</code>. Same <code>SELECT</code>, same result, different storage, no changes, no fuss.</p></li><li><p>You can change the actual byte format in storage. In PostgreSQL, the <strong>table access method is pluggable</strong> (Citus columnar, TimescaleDB). SQL Server flips a table from rows to columns with a clustered columnstore index. Oracle does it with Hybrid Columnar Compression, or stores the whole table inside a B-tree as an index-organized table instead of a heap. Same logical table, same query — completely different bytes on disk.</p></li><li><p>In MySQL you can <strong>swap the storage engine</strong> out from under a table entirely: <code>ALTER TABLE t ENGINE=...</code> moves a table between InnoDB (B-trees on disk), MyISAM, Archive (compressed), CSV (a literal text file), or MEMORY (pure RAM). One table, one query, completely different machinery underneath.</p></li><li><p>The data doesn&#39;t even have to be in local storage at all. In PostgreSQL, make it a <code>FOREIGN TABLE</code> over an FDW (Foreign Data Wrappers) and the rows live happily on <strong>another machine entirely</strong> — the query doesn&#39;t even care or know. Oracle reads flat files as external tables; DuckDB queries Parquet files on disk as if they were tables. The data is somewhere else, on a remote machine - SQL is the same.</p></li><li><p>It doesn&#39;t even have to be one machine. Hand it to a distributed SQL engine — CockroachDB, TiDB, Spanner — and your data becomes a replicated, sharded key-value store smeared <strong>across a cluster</strong>. You have no idea which node, let alone which disk, holds any given row. CockroachDB speaks Postgres&#39;s wire protocol so you can use standard PostgreSQL unchanged, TiDB speaks MySQL&#39;s, and you still write ordinary SQL. Or push it to the cloud, where Snowflake keeps everything as columnar micro-partitions in object storage. Same <code>SELECT</code>, same result, no fuss.</p></li><li><p>And finally — no disk at all. Who says we need disks? Declare a MySQL table <code>ENGINE=MEMORY</code>, run PostgreSQL on a <code>tmpfs</code> with the <strong>whole data directory in RAM</strong>, switch on SQL Server&#39;s In-Memory OLTP, open SQLite as <code>:memory:</code>. No persistent storage anywhere. Same query, same result — and the thing is still, unmistakably, a database.</p></li></ul><p>Martin writes: <em>&quot;To mitigate the time delay imposed by disks, you need indexes...&quot;</em>. Then why are in-memory databases full of indexes? Why are they (in-memory databases) a thing at all? Because an index was never about the medium - it is about not scanning every row when you want one, be it on disk or in RAM.</p><p>As we can see, different devices, different formats, even machines and clusters, or even no machines at all — the same logical model, the same SQL, the same results. The RDBMS already abstracts storage for you. It is designed to do exactly that. Thank you very much, but you are wrong.</p><h3 id="the-cost-1" tabindex="-1">The Cost <a class="header-anchor" href="#the-cost-1" aria-label="Permalink to &quot;The Cost&quot;">​</a></h3><p>Cost is <strong>never ending persistence-ceremony</strong>. We are asked to carefully construct our data models in memory (to maintain the illusion of encapsulation), protect it from illogical and unwanted changes, and then save it to the database (persist it) when the time is right.</p><p>It is hard to overstate how big this persistence-ceremony thing really is. The number of specialized patterns, libraries, countless tutorials, videos, entire philosophies, strategies, etc. But take a look at this trivial example:</p><p>This is a simple SQL statement to update a transfer approval - two people must sign off, they must be different people, and the status moves from pending to partly to fully approved. In a traditional codebase this would be a considerable chunk of code spanning multiple files and doing several database calls, just to maintain the illusion.</p><details class="code-collapsible" open><summary>sql</summary><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes catppuccin-latte catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">update</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> transfer_approvals</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">set</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">  status</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> =</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> case</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    when</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> status</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> =</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;"> &#39;pending&#39;</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> then</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;"> &#39;partly_approved&#39;</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    when</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> status</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> =</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;"> &#39;partly_approved&#39;</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">         and</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> approver1 </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">is</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> distinct</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> from</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> $</span><span style="--shiki-light:#FE640B;--shiki-dark:#FAB387;">1</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> then</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;"> &#39;fully_approved&#39;</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    else</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> status</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">  end</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">,</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  approver1 </span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;">=</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> case</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> when</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> status</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> =</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;"> &#39;pending&#39;</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> then</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> $</span><span style="--shiki-light:#FE640B;--shiki-dark:#FAB387;">1</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> else</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> approver1 </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">end</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">,</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  approver2 </span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;">=</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> case</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    when</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> status</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> =</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;"> &#39;partly_approved&#39;</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">         and</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> approver1 </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">is</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> distinct</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> from</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> $</span><span style="--shiki-light:#FE640B;--shiki-dark:#FAB387;">1</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> then</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> $</span><span style="--shiki-light:#FE640B;--shiki-dark:#FAB387;">1</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    else</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> approver2</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">  end</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">where</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> </span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  transfer_id </span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;">=</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> $</span><span style="--shiki-light:#FE640B;--shiki-dark:#FAB387;">2</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">  and</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> status</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> in</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> (</span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;">&#39;pending&#39;</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">, </span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;">&#39;partly_approved&#39;</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">)</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">returning </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">status</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div></details><p>We don&#39;t know where those transfer approvals are. Is it stored on a disk? What disk is it on? Is it in memory, what format it&#39;s in, or even what machine it is on, and we don&#39;t care. That is not the point. This is a DECLARATION. We have just declared business rules for how to update transfer approvals.</p><p>It has nothing to do with storage as far as the application is concerned. And since our RDBMS engine hides and abstracts all the storage details and does all the persistence work for us, what are we left with then? That&#39;s right: <strong>the business logic and business rules</strong>. Just a declaration of how our transfer approvals should be updated in this case. Nothing else. No ceremony, no plumbing, no persistence layer, no storage, no nothing.</p><p>The entire pile of code which is now gone with this move is the following:</p><ul><li>the entity / aggregate class (the in-memory model)</li><li>the repository (to fetch it and put it back)</li><li>the unit-of-work / change tracker (to know which fields are dirty)</li><li>the O/R mapping config (to translate object &lt;-&gt; row)</li><li>the load → mutate-in-memory → save dance</li><li>the surrounding transaction scope</li></ul><p>Every one of those exists for one reason only: to carry state from memory to storage and back. Remove that silly belief, and every one of those big code blocks removes itself.</p><h2 id="_3-data-structures-abstraction-misconception" tabindex="-1">3) Data Structures Abstraction Misconception <a class="header-anchor" href="#_3-data-structures-abstraction-misconception" aria-label="Permalink to &quot;3) Data Structures Abstraction Misconception&quot;">​</a></h2><h3 id="the-claim-2" tabindex="-1">The Claim <a class="header-anchor" href="#the-claim-2" aria-label="Permalink to &quot;The Claim&quot;">​</a></h3><p>If we already have established two previous claims — that the state lives in memory and that the database is a storage device — then data itself must be an in-memory data structure as well. And, if we are going to perform operations on it, protect the state, mutate, and so on, then we need to have it in memory, and only suitable in-memory data structures will do, period.</p><p>Direct quote from Eric Evans, Domain-Driven Design (2003), Part II &quot;Building Blocks of a Model-Driven Design,&quot; Chapter 6 &quot;The Lifecycle of a Domain Object,&quot; in the section on Repositories (p. 108), he writes:</p><blockquote><p>For each type of object that needs global access, create an object that can provide the <strong>illusion of an in-memory collection</strong> of all objects of that type.</p></blockquote><figure style="margin:1em 0;text-align:center;"><img src="`+o+'" alt="Screenshot from Eric Evans, Domain-Driven Design (2003), p. 108, showing the Repository definition" style="display:block;max-width:100%;height:auto;margin:0 auto;"><figcaption style="font-size:0.8em;color:#888;margin-top:0.5em;"> Eric Evans, <em>Domain-Driven Design</em> (2003), p. 108. </figcaption></figure><p>The word Evans uses: <strong>illusion</strong>. Not a real collection, oh no, just the <em>illusion</em> of an in-memory structure. This should tell you everything now: You should only build an elaborate pattern to <strong>simulate an in-memory collection</strong> if the real data collection was never in memory to begin with.</p><p>The Repository exists precisely because the objects live in the database, and its entire job is to make them <em>look</em> like they are sitting in memory. Fake it until you make it, except you will never make it.</p><p>The repository pattern is an admission in structural form: relational data, dressed up to pass as an in-memory data structure (so we can do OOP on it).</p><p>This is not a coincidence nor an isolated quote. For example, in the book <em>Patterns of Enterprise Application Architecture</em> (2002), Martin Fowler writes (source: <a href="https://martinfowler.com/eaaCatalog/repository.html" target="_blank" rel="noreferrer">https://martinfowler.com/eaaCatalog/repository.html</a>):</p><blockquote><p>A Repository mediates between the domain and data mapping layers, <strong>acting like an in-memory domain object collection</strong>.</p></blockquote><p>Acting like an in-memory domain object collection? Why do we want to force relational data into an in-memory collection? Just for good measure, let&#39;s check out Microsoft&#39;s recommendation in their <a href="https://learn.microsoft.com/en-us/dotnet/architecture/microservices/microservice-ddd-cqrs-patterns/infrastructure-persistence-layer-design" target="_blank" rel="noreferrer">Software Architecture e-book</a>, in a part that teaches us how to &quot;Design the infrastructure persistence layer.&quot; Microsoft echoes Fowler almost word for word, putting it as a set of domain objects in memory.</p><blockquote><p>A repository performs the tasks of an intermediary between the domain model layers and data mapping, acting in a similar way to a set of domain objects in memory.</p></blockquote><p>And then they continue:</p><blockquote><p>Basically, a repository allows you to populate data in memory that comes from the database in the form of the domain entities. Once the entities are in memory, they can be changed and then persisted back to the database through transactions.</p></blockquote><p>Again, just goes to prove the point above - real state is in the database, we are simply mandated to load temporary chunks into memory - in order to maintain the illusion of encapsulation and illusion of in-memory data structure.</p><p>Perhaps we might end up with an illusion of the entire software solution?</p><p>In any case, more than a decade after Evans and Fowler had laid out this machinery that simulates in-memory collections, the mismatch still had not been solved. We know this because in 2014, the field&#39;s leading DDD experts from around the world gathered at the DDD eXchange conference in NYC to figure out how to do DDD better and, finally, solve this Object-Relational Impedance Mismatch. Because you do not hold a summit to solve a problem you have already solved.</p><p>One of the speakers, renowned DDD expert Vaughn Vernon, gave a talk called <a href="https://kalele.io/the-ideal-domain-driven-design-aggregate-store/" target="_blank" rel="noreferrer">&quot;The Ideal Domain-Driven Design Aggregate Store?&quot;</a> where he proposed a final solution to the O/R Impedance Mismatch problem:</p><blockquote><p>During the park bench discussion I promoted the idea of serializing Aggregates as JSON and storing them in that object notation in a document store. A JSON-based store would enable you to query the object’s fields. Central to the discussion, there would be no need to use an ORM. This would help to keep the Domain Model pure and save days or weeks of time generally spent fiddling with mapping details. Even more, your objects could be designed in just the way your Ubiquitous Language is developed, and without any object-relational impedance mismatch whatsoever. Anyone who has used ORM with DDD knows that the limitations of mapping options regularly impede your modeling efforts.</p></blockquote><figure style="margin:1em 0;text-align:center;"><img src="'+r+'" alt="Screenshot from Vaughn Vernon, &#39;The Ideal Domain-Driven Design Aggregate Store?&#39;, proposing JSON-serialized Aggregates in a document store" style="display:block;max-width:100%;height:auto;margin:0 auto;"><figcaption style="font-size:0.8em;color:#888;margin-top:0.5em;"> Vaughn Vernon, <a href="https://kalele.io/the-ideal-domain-driven-design-aggregate-store/">&quot;The Ideal Domain-Driven Design Aggregate Store?&quot;</a></figcaption></figure><p>Framing in this case is that the O/R mapping tools and libraries are the main source of impedance mismatch, and if we could just get rid of them, then we would have solved the problem. No O/R mapping, no O/R mapping at all, and no impedance mismatch. That&#39;s the idea. In essence, the proposed data design is this:</p><ul><li>Serialize each aggregate Object to JSON, store it as a blob</li><li>Every table is just (id, data json) — a key and a blob, nothing else</li><li>&quot;Reference Other Aggregates By Identity Only&quot; — no foreign keys, no joins, nothing, each blob standalone</li></ul><p>That&#39;s it. That is the &quot;ideal aggregate store&quot; solution to the impedance mismatch problem.</p><p>Now, to be fair, Vaughn Vernon is not saying that RDBMS = file system. He does propose using Postgres for ACID and JSON querying. He does want to keep the relational engine. Just not the relational model.</p><p>And that is the whole trick. We have solved the Object-Relational impedance mismatch by removing the relational part entirely — and keeping the Object part, obviously. No relations, no foreign keys, no joins, no set operations, no nothing. Just a flat collection of objects, serialized, frozen to disk, and fetched by id. The illusion of an in-memory collection, made real at last.</p><p>So, yeah, the solution to the O/R impedance mismatch is to remove the R (relational part) entirely, and just have a key-value store with JSON blobs.</p><p>That was proposed ten years ago. Does anyone use that today? Is that how the industry builds? I don&#39;t think so. Twenty years after Evans and Fowler, the mismatch sits exactly where it started. We never solved it.</p><h3 id="the-reality-2" tabindex="-1">The Reality <a class="header-anchor" href="#the-reality-2" aria-label="Permalink to &quot;The Reality&quot;">​</a></h3><p>Let&#39;s get one thing out of the way immediately, because I don&#39;t want to win this argument by cheating.</p><p>A table is a set. That is not a metaphor — it is the mathematical definition. Codd&#39;s 1970 paper defines a relation as a subset of the Cartesian product of domains: a set of tuples.</p><figure style="margin:1em 0;text-align:center;"><img src="'+l+`" alt="Screenshot from E.F. Codd, &#39;A Relational Model of Data for Large Shared Data Banks,&#39; defining a relation as a subset of the Cartesian product of domains" style="display:block;max-width:100%;height:auto;margin:0 auto;"><figcaption style="font-size:0.8em;color:#888;margin-top:0.5em;"> Codd, E.F., &quot;A Relational Model of Data for Large Shared Data Banks,&quot; CACM 13(6), June 1970, pp. 377–387, §1.3. </figcaption></figure><p>And SQL is an algebra over those sets — selection, projection, <code>JOIN</code>, <code>UNION</code>, <code>INTERSECT</code>, <code>EXCEPT</code> — a closed algebra, so every operation over sets returns another set you can keep operating on.</p><p>All true. And here is the problem with building the argument on that: an in-memory collection can do all of it too. LINQ in C# ships <code>Join</code>, <code>GroupJoin</code>, <code>Union</code>, <code>Intersect</code>, <code>Except</code>, <code>Distinct</code>, <code>GroupBy</code>. Those are not arbitrary method names — those are Codd&#39;s operators, reimplemented over <code>IEnumerable</code>. And every other ecosystem rebuilt some version of the same algebra over its collections; LINQ just did it most completely.</p><p>So if the impedance mismatch were about operations — about what you can <em>do</em> with the data — it would have been solved around 2007, when the operators finished porting. Case closed, everybody go home.</p><p>The mismatch is still here. Which means it was never about the operations.</p><p>Here is what the table has that no collection in your process can have. Not what it <em>does</em> — what it <strong>is</strong>:</p><ol><li><p><strong>It is the record.</strong> As established in the first misconception: the row is not a representation of the state, it is the state. Your collection is a copy of it, taken at load time.</p></li><li><p><strong>It is shared.</strong> Every process, every writer, every background job operates on the same table, under the engine&#39;s arbitration. Your collection is private to one process. The other writers don&#39;t know it exists, and they are not waiting for it.</p></li><li><p><strong>It is durable and live.</strong> The table existed before your process started, will exist after it dies, and keeps changing under other writers the whole time. Your collection is a photograph. The table is the thing being photographed — and it kept moving after the shutter clicked.</p></li><li><p><strong>It is guarded.</strong> <code>CHECK</code>, <code>UNIQUE</code>, <code>NOT NULL</code>, foreign keys — enforced transactionally, against every writer, no exceptions. Your in-memory validation binds exactly one thing: your copy. The next writer does not inherit your discipline.</p></li></ol><p>And now the key observation, the one this whole chapter hangs on: <strong>not one of these four is an operation.</strong></p><p>LINQ could port <code>Join</code> because join is a function — values in, values out. Pure computation travels; you can implement it anywhere. Being the shared, durable, guarded record is not a function. There is no method you can add to <code>List&lt;T&gt;</code> that makes it <em>be</em> the authoritative state. You can port an operator. You cannot port a status.</p><p>Notice where the line falls between what made the trip into memory and what didn&#39;t. Everything that is algebra — join, filter, group, project — ported over just fine. Everything that is state — the transaction, the arbitration, the constraints, the durability — never left the database. It can&#39;t. The line between them is exactly the line we drew in the first misconception: computation versus state.</p><p>And if you want this confirmed by the industry itself: the most serious application of LINQ&#39;s relational operators is LINQ-to-Entities, which takes your C# expression tree and compiles it <em>back into SQL</em>, to send to the database. We rebuilt the algebra in memory, and its main job turned out to be translating itself back — because the algebra made the trip, and the data never did.</p><p>Which puts the domain object model in a completely new light. It is not an alternative data structure for your data. Look at the machinery again, piece by piece:</p><div class="table-container"><div class="table-wrapper"><table tabindex="0"><thead><tr><th>The object model builds</th><th>To simulate</th></tr></thead><tbody><tr><td>Repository</td><td>the table</td></tr><tr><td>Identity map</td><td>the primary key</td></tr><tr><td>Navigation properties</td><td>foreign keys and joins</td></tr><tr><td>Unit of work</td><td>the transaction</td></tr><tr><td>Change tracker</td><td>what <code>UPDATE ... SET</code> already knew</td></tr><tr><td>In-memory validation</td><td><code>CHECK</code>, <code>UNIQUE</code>, and foreign key constraints</td></tr></tbody></table></div></div><p>That is not a different model. That is the same model — the database — re-implemented in RAM, minus the four properties that made it meaningful. The domain model is a simulation of the database, running inside your process. Evans told us himself, remember: the <em>illusion</em> of an in-memory collection. And the real thing sits two feet away the entire time, doing all of it correctly, under ACID, for every process at once. We call the copy &quot;the domain&quot; and the authority &quot;a detail.&quot;</p><p>To be precise about what I am <em>not</em> claiming, before somebody builds a strawman out of it:</p><p>In-memory data structures are not the problem. Pure computation over values you were genuinely given is exactly what memory is for — take inputs, compute, return outputs. The game scene and the compiler syntax tree from the first misconception live in memory legitimately, because they <em>are</em> the state of those systems. LINQ over data you rightfully hold is wonderful.</p><p>Caching is not the problem either — honest caching. A cache knows it is a copy. It has a TTL, an invalidation story, and it never claims to be the truth.</p><p>The sin is narrow and specific: <strong>a copy that claims to be the authority and has no answer for the moment it goes stale.</strong> That is not a cache, and it is not a data-structure choice. That is a simulation impersonating the thing it copied.</p><p>Hold on to that word — <em>impersonating</em> — because it explains something two decades of framework engineering could not fix. Every famous ORM pathology you have ever fought is not a separate bug with a separate fix. It is one failure with many faces: a simulation forced to behave like the authority it impersonates. Let&#39;s count the faces.</p><h3 id="the-cost-2" tabindex="-1">The Cost <a class="header-anchor" href="#the-cost-2" aria-label="Permalink to &quot;The Cost&quot;">​</a></h3><p>What follows are not four independent grievances. It is one impossibility wearing four costumes.</p><h4 id="copy-not-record-→-staleness-and-write-amplification" tabindex="-1">Copy, not record → staleness and write amplification <a class="header-anchor" href="#copy-not-record-→-staleness-and-write-amplification" aria-label="Permalink to &quot;Copy, not record → staleness and write amplification&quot;">​</a></h4><p>The object must be hydrated before it can be mutated — that is the whole contract. So to change one column, the ORM does a <code>SELECT</code> followed by an <code>UPDATE</code>: load the row (usually the whole aggregate — scroll back to that Reddit picture and count the objects), let the object &quot;decide&quot; in memory, write it back. Remember the transfer approval from the previous chapter: one <code>UPDATE</code> statement, zero preliminary round trips, the rule declared right where the state lives. The simulated version is load → check in memory → mutate in memory → save. And between load and save, the row is free to change under you.</p><p>Hence lost updates. Hence the &quot;optimistic concurrency token&quot; — a <code>rowversion</code> or <code>xmin</code> column added to the schema not because the business needs it, but to detect that your copy lied to you between load and save. The token is a confession written in DDL: <em>the copy is not the record, and we know it.</em></p><h4 id="private-not-shared-→-arbitration-lives-in-the-database-anyway" tabindex="-1">Private, not shared → arbitration lives in the database anyway <a class="header-anchor" href="#private-not-shared-→-arbitration-lives-in-the-database-anyway" aria-label="Permalink to &quot;Private, not shared → arbitration lives in the database anyway&quot;">​</a></h4><p>Two requests load the same invoice. Each holds a private snapshot; each decides from it; both decisions are &quot;valid&quot; in memory, and one of them is wrong in reality. The decision your domain model makes is provisional — the only binding decision happens in the database, under a lock, inside a transaction, where arbitration has lived all along. The version-token &quot;rescue&quot; concedes exactly this: the object proposes, but the <code>WHERE</code> clause of the write disposes. Now add a background job, a second service, a nightly import — and the in-memory &quot;state&quot; stops being state at all. It is one process&#39;s guess about what the state was, some number of milliseconds ago. (Much more on this in the concurrency and integrity misconception.)</p><h4 id="graph-walk-not-set-operation-→-the-access-pattern-tax" tabindex="-1">Graph walk, not set operation → the access-pattern tax <a class="header-anchor" href="#graph-walk-not-set-operation-→-the-access-pattern-tax" aria-label="Permalink to &quot;Graph walk, not set operation → the access-pattern tax&quot;">​</a></h4><p>We conceded that collections have the operators. But the object model&#39;s shape pushes you away from them: objects <em>navigate</em>. <code>customer.Orders</code>, <code>order.Lines</code> — walking references one object at a time, each step a query you didn&#39;t see, fired from behind a property getter. One <code>JOIN</code> becomes a hundred <code>SELECT</code>s, and the call site looks innocent. This is N+1, the most documented performance pathology of the last two decades. And the fixes bill you separately: eager-load with <code>Include</code> and over-fetch half the database, or project into DTOs — at which point you are writing relational queries again, in C#, so that a library can compile them back into the SQL you were abstracting away. The abstraction gets abandoned at exactly the moment it gets tested.</p><h4 id="simulation-not-engine-→-the-capability-ceiling" tabindex="-1">Simulation, not engine → the capability ceiling <a class="header-anchor" href="#simulation-not-engine-→-the-capability-ceiling" aria-label="Permalink to &quot;Simulation, not engine → the capability ceiling&quot;">​</a></h4><p>Window functions. Recursive CTEs — foreign keys form a graph, and the engine will walk arbitrary-depth hierarchies for you, declaratively. <code>GROUPING SETS</code>, lateral joins, partial and expression indexes, set-based bulk updates. And underneath all of it: a cost-based planner with live statistics about <em>your actual data</em>, choosing between a hash join, a merge join, and an index scan — per query, per data distribution. The simulation has none of this and cannot grow it. This one isn&#39;t even a defect — it is simply what a private copy in RAM lacks next to a database engine.</p><p>The standard reply — &quot;but EF has raw SQL escape hatches&quot; — is my argument wearing a different hat. If the object model were the system of record, there would be nothing to escape <em>to</em>. The moment you drop to SQL for the hard 20%, you have admitted where the real system was all along. The escape hatch is not a counterexample. It is the proof.</p><hr><p>Now step back and look at the four together. None of them is an implementation defect. Hibernate is twenty-five years old; Entity Framework is eighteen. Some of the best engineers in the industry have been sanding these edges for two decades, and every one of these problems is still here — because they are not bugs in the simulation. They are the simulation <em>working correctly</em>: behaving exactly like what it is — a private, transient copy — instead of what it plays: the shared, durable record. That gap does not close with effort, because it is not made of code. It is made of what the two things <em>are</em>.</p><p>And that is Neward&#39;s quagmire, stated mechanically. An escalating investment that cannot win — not because the enemy is strong, but because a copy cannot out-invest its way into being the original.</p><h2 id="_4-abstraction-over-algorithms" tabindex="-1">4) Abstraction Over Algorithms <a class="header-anchor" href="#_4-abstraction-over-algorithms" aria-label="Permalink to &quot;4) Abstraction Over Algorithms&quot;">​</a></h2><h3 id="the-claim-3" tabindex="-1">The Claim <a class="header-anchor" href="#the-claim-3" aria-label="Permalink to &quot;The Claim&quot;">​</a></h3><p>Back in the Wikipedia section, I promised that behavior is a different problem axis and that we would come to it when we get to algorithms. Here we are.</p><p>The first three misconceptions were about data. This one is about behavior. The claim goes like this: fine, the data may sit in the database — but the algorithms, the logic, the behavior of the system, those belong in application code. SQL fetches; code computes. The database is where data sleeps, and the application is where it wakes up.</p><p>Martin Fowler wrote an article about this exact question back in February 2003, called <a href="https://martinfowler.com/articles/dblogic.html" target="_blank" rel="noreferrer">&quot;Domain Logic and SQL&quot;</a>. It opens with an honest description of the mainstream attitude:</p><blockquote><p>Many application developers, particularly strong OO developers like myself, tend to treat relational databases as a storage mechanism that is best hidden away.</p></blockquote><p>There it is again — the storage device from the second misconception — but this time the subject is logic. To his credit, Fowler takes the SQL option far more seriously than most of his readers ever did. And still, the verdict:</p><blockquote><p>Personally I don&#39;t think performance should be the first question. My philosophy is that most of the time you should focus on writing maintainable code.</p></blockquote><p>With a warning label attached:</p><blockquote><p>I would suggest that if you go the route of putting a lot of logic in SQL, don&#39;t expect to be portable — use all of your vendors extensions and cheerfully bind yourself to their technology.</p></blockquote><p>And a concession that defines SQL&#39;s proper place in this worldview:</p><blockquote><p>If you use an in-memory approach and have hot-spots that can be solved by more powerful queries, then do that.</p></blockquote><p>So there is the claim, in its most reasonable and balanced form, from its most reasonable and balanced proponent: logic in memory is the default — maintainable, portable, testable. Logic in SQL is the exception — a performance hot-fix, to be applied reluctantly, hot-spot by hot-spot. Twenty-three years later, this is still the mainstream position, and most codebases you will open this week are built on it.</p><h3 id="the-reality-3" tabindex="-1">The Reality <a class="header-anchor" href="#the-reality-3" aria-label="Permalink to &quot;The Reality&quot;">​</a></h3><p>SQL is a programming language. A declarative one, but a programming language.</p><p>Look back at the transfer approval statement from the second misconception. Two approvers, they must be different people, the status walks from pending to partly to fully approved. That is not &quot;fetching data.&quot; That is behavior — an algorithm, expressed as a declaration, executed next to the data, atomically.</p><p>Now ask what the algorithms of a business system actually are. Strip away the ceremony and it is overwhelmingly this: filter, join, group, aggregate, rank, deduplicate, walk a hierarchy, compute something over ordered data. Which is, item for item, exactly what SQL was designed to express. A running balance, for example:</p><details class="code-collapsible" open><summary>sql</summary><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes catppuccin-latte catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">select</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  customer_id,</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  transaction_date,</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  amount,</span></span>
<span class="line"><span style="--shiki-light:#1E66F5;--shiki-light-font-style:italic;--shiki-dark:#89B4FA;--shiki-dark-font-style:italic;">  sum</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">(amount) </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">over</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> (</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    partition</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> by</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> customer_id</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    order by</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> transaction_date, transaction_id</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  ) </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">as</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> running_balance</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">from</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> transactions;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div></details><p>The in-memory version of this algorithm: fetch every transaction over the wire, group by customer in a dictionary, sort each group, loop and accumulate — plus the memory footprint, plus deciding what happens on the day the table stops fitting in RAM. The declarative version is the window function above. Ranking, top-N-per-group, gaps in sequences, running totals, year-over-year — window functions. Org charts, bills of materials, category trees — recursive CTEs: declare the traversal, and the engine walks the graph. With recursive CTEs, SQL is Turing-complete — not that you should compute Fibonacci in it, but &quot;SQL can&#39;t express my logic&quot; stopped being true decades ago.</p><p>But here is the part that actually settles the argument, and it is not expressiveness. When you write the loop, you are writing one algorithm, frozen at commit time. When you write the declaration, the engine writes the algorithm — at runtime, with a cost-based planner and live statistics about your actual data. Hash join, merge join, nested loop; index scan or sequential scan; parallel workers or not — chosen per query, per data distribution, and re-chosen as the data grows. Your hand-written loop was a perfectly good plan at ten thousand rows. At ten million it is a catastrophe, and it will not adapt, because it is code — someone has to notice it, profile it, and rewrite it. The declaration just quietly gets a new plan.</p><p>Nobody would hand-write three join algorithms plus a statistics-driven optimizer to choose between them in the service layer. That machinery already exists. It sits directly under the data — and the claim instructs us not to use it.</p><p>Which leaves the claim&#39;s justifications, so let&#39;s take them in order. Maintainability: is the eight-line window function really less maintainable than the same algorithm spread across a repository, a service method, and a mapping profile? &quot;Maintainable&quot; is not a synonym for &quot;written in my favorite language.&quot; Portability: the second misconception already dealt with that — the core of SQL is an ANSI/ISO standard that runs on every engine, while your domain layer is portable to exactly nothing; nobody in recorded history has swapped C# + EF for Java + Hibernate because the code was so nicely decoupled. Testability: SQL is testable — pgTAP exists, and the oldest trick in the book still works: open a transaction, run the test, roll back. Besides, when you mock the database out of a test of data logic, look at what is left standing: you are testing the simulation from the previous misconception, not the system.</p><h3 id="the-cost-3" tabindex="-1">The Cost <a class="header-anchor" href="#the-cost-3" aria-label="Permalink to &quot;The Cost&quot;">​</a></h3><p>Every other corner of the industry has a name for this cost: moving data to compute instead of moving compute to data. The entire big-data field was built on the lesson that you ship the algorithm to where the data lives, because the other direction does not scale. Business software orthodoxy teaches the other direction as a best practice.</p><p>So we pay, in four installments:</p><ul><li><p><strong>The wire tax.</strong> Rows are read, serialized, shipped across the network, deserialized, and mapped into objects — so that a loop can run in the application, redoing work the engine would have done in place, with indexes.</p></li><li><p><strong>The round-trip tax.</strong> Iterative logic in the application is chatty by nature: a query per step, per entity, per iteration. The N+1 problem from the previous chapter is this tax&#39;s most famous invoice.</p></li><li><p><strong>The reimplementation tax.</strong> Every in-memory group, sort, join, and aggregate is a worse copy of what the engine already had: no indexes, no statistics, no planner, no parallelism, and memory bounded by your heap.</p></li><li><p><strong>The frozen plan tax.</strong> The hand-written algorithm does not adapt to data growth. It just gets slower, quietly, until the nightly job that took a minute takes six hours, and someone gets paged to rediscover this chapter.</p></li></ul><p>And the punchline is already inside the claim itself. SQL is admitted as the exception, for hot-spots — and then, over the life of the system, every part that matters turns out to be a hot-spot. One by one, the pieces that count get rewritten in SQL anyway, by tired people, during incident reviews. It is the same concession we saw with the raw-SQL escape hatch in the previous misconception: the exception clause ends up doing all the load-bearing work. At some point, the honest question is why the exception is not the architecture.</p><h2 id="_5-abstraction-over-concurrency-and-integrity" tabindex="-1">5) Abstraction Over Concurrency and Integrity <a class="header-anchor" href="#_5-abstraction-over-concurrency-and-integrity" aria-label="Permalink to &quot;5) Abstraction Over Concurrency and Integrity&quot;">​</a></h2><h3 id="the-claim-4" tabindex="-1">The Claim <a class="header-anchor" href="#the-claim-4" aria-label="Permalink to &quot;The Claim&quot;">​</a></h3><p>DDD&#39;s answer to data integrity is the <strong>aggregate</strong>. Eric Evans, Domain-Driven Design, Chapter 6:</p><blockquote><p>An AGGREGATE is a cluster of associated objects that we treat as a unit for the purpose of data changes.</p></blockquote><p>The aggregate root guards the boundary and enforces the invariants — the business rules that must never be broken. That Reddit picture from the first misconception is exactly this: <code>Article</code> at the root, guarding its packages, price periods, and quantity ranges, with <code>isValid()</code> standing watch.</p><p>Vaughn Vernon — the same Vaughn Vernon from the aggregate store — codified the discipline in <em>Implementing Domain-Driven Design</em> (2013) and the <a href="https://www.dddcommunity.org/wp-content/uploads/files/pdf_articles/Vernon_2011_1.pdf" target="_blank" rel="noreferrer">&quot;Effective Aggregate Design&quot;</a> essays it grew from. He is admirably precise about it:</p><blockquote><p>An invariant is a business rule that must always be consistent.</p></blockquote><blockquote><p>A properly designed Aggregate is one that can be modified in any way required by the business with its invariants completely consistent within a single transaction.</p></blockquote><blockquote><p>Thus, Aggregate is synonymous with transactional consistency boundary.</p></blockquote><p>Along with the rule that turns it into a discipline:</p><blockquote><p>A properly designed Bounded Context modifies only one Aggregate instance per transaction in all cases.</p></blockquote><p>So the claim: invariants — the rules that must <em>always</em> hold — are enforced by the domain model, in memory, one aggregate at a time. The community even has a slogan for it: the <em>always-valid domain model</em>.</p><p>But read Vernon&#39;s third sentence again, slowly, because something remarkable is happening in it. &quot;Aggregate is synonymous with <em>transactional consistency boundary</em>.&quot; The pattern defines itself as a transaction. Hold that thought.</p><h3 id="the-reality-4" tabindex="-1">The Reality <a class="header-anchor" href="#the-reality-4" aria-label="Permalink to &quot;The Reality&quot;">​</a></h3><p>First things first: the goal is completely right. Invariants must hold — that was never in dispute; it is the same reasonable instinct we already conceded in the first misconception. The question was never <em>whether</em> to enforce invariants. The question is where enforcement actually binds.</p><p>An invariant enforced in memory binds exactly one process — property four of the data structures misconception: the next writer does not inherit your discipline. And it binds at exactly one moment — validation time. Between the check and the write, the world keeps moving.</p><p>The canonical example, the one every team eventually learns in production: usernames must be unique. The domain model checks — no such username, valid — and inserts. Two concurrent registrations both check, both pass, both insert. The &quot;always-valid&quot; model just produced invalid data, twice, without a single line of it misbehaving. Check-then-act on a private snapshot is a race by construction — TOCTOU, time-of-check to time-of-use, a bug class old enough to have its own acronym. And notice what every team actually does about it: they add a <code>UNIQUE</code> constraint. The engine catches what the model cannot.</p><p>It is worth being precise about <em>why</em> the engine can do what the model cannot: it is the only party that sees every writer. Which makes it the only place where invariant machinery means anything: <code>NOT NULL</code>, <code>CHECK</code>, <code>UNIQUE</code>, foreign keys, <code>EXCLUDE</code> — wrapped in transactions, with isolation levels up to <code>SERIALIZABLE</code>, where concurrent transactions are guaranteed to behave as if they had run one at a time. Enforced against every writer: your application, the second instance behind the load balancer, the background job, the DBA at 2 a.m. No exceptions, and no discipline required.</p><p>Time to pay the debt from the first misconception. The hardest invariant in that Reddit aggregate: price periods must not overlap. <code>isValid()</code> can inspect its own copy — while another process commits an overlapping period it has never heard of. Here is the entire invariant, declared:</p><details class="code-collapsible" open><summary>sql</summary><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes catppuccin-latte catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">create</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> extension </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">if</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> not</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> exists</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> btree_gist;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">alter</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> table</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> price_periods</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">add</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;"> constraint</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> price_periods_no_overlap</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">exclude </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">using</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> gist (</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  package_id </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">with</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> =</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">,</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">  daterange(date_begin, date_end, </span><span style="--shiki-light:#40A02B;--shiki-dark:#A6E3A1;">&#39;[]&#39;</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">) </span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">with</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> &amp;&amp;</span></span>
<span class="line"><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div></details><p>Two periods for the same package with overlapping date ranges can now <em>not exist</em>. Not &quot;will be caught, provided the request comes in through the domain layer&quot; — cannot exist. Under any concurrency, from any writer, forever. One declaration. That is what enforcing an invariant actually means.</p><p>And now unhold that thought from the claim. <em>Aggregate is synonymous with transactional consistency boundary.</em> The transaction is a database concept. The pattern&#39;s own definition concedes that invariant enforcement is transaction work — it just redraws the transaction as an object graph, in one process&#39;s private memory, where it can see no other writer and therefore enforce nothing. The aggregate is a hand-drawn picture of a transaction. The database has the real ones — and the real ones can span whatever rows and tables the invariant actually needs, not just one object cluster.</p><h3 id="the-cost-4" tabindex="-1">The Cost <a class="header-anchor" href="#the-cost-4" aria-label="Permalink to &quot;The Cost&quot;">​</a></h3><p><strong>Everything is enforced twice — or worse, once, in the wrong place.</strong> The same rules live in C# validation and in database constraints, drifting apart release by release — the behavioral edition of the duplicated models from the first misconception, same bill, new line item. And the team that takes &quot;always-valid&quot; at its word and skips the constraints has it worse: their invariants are now enforced nowhere. They hold only in the absence of concurrency — which is to say, they are not enforced. They are observed, until further notice.</p><p><strong>The races ship.</strong> Check-then-act bugs pass every unit test, because in the test the domain model really is alone — the race needs a second writer, and the test suite proudly mocks that out. The mock removes the exact enemy the invariant exists to fight. So the bug appears only under production load, intermittently, and is discovered as corrupted data weeks later: the duplicate payment, the double-booked slot, the negative stock. Every experienced engineer has one of these stories, and in every single one of them, the domain model passed all of its tests.</p><p><strong>The aggregate-boundary industry.</strong> Vernon&#39;s rule — one aggregate instance per transaction — voluntarily outlaws the multi-row, multi-table atomicity the engine offers natively. So what happens when a real invariant spans two aggregates? A whole discipline unfolds: redesign the boundaries, or accept <em>eventual consistency</em> between aggregates, coordinated through domain events, process managers, sagas, compensating actions. An entire detect-and-compensate machinery, invented to route around <code>BEGIN ... COMMIT</code>. The database would have held both rows in one transaction — the real kind — and gone to lunch.</p><p>And with that, the five misconceptions close into a single picture. The state lives in the database (1), which already abstracts its own storage (2). Its tables cannot be replaced by in-memory structures — only impersonated by them (3). Its language already expresses the algorithms (4), and its transactions and constraints are the only invariant enforcement that actually binds (5). Every axis the modern application layer re-implements is an axis the engine already owns. The impedance mismatch was never a mapping problem between two equal worlds. It is the ongoing cost of running a simulation of one world inside the other — and calling the original &quot;a detail.&quot;</p>`,213)]))}const f=t(h,[["render",d]]);export{b as __pageData,f as default};
