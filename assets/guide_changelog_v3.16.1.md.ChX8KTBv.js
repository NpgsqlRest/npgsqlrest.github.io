import{_ as t,c as a,o as s,a5 as i}from"./chunks/framework.CgT1UzWm.js";const g=JSON.parse('{"title":"Changelog v3.16.1 (2026-06-01)","description":"NpgsqlRest v3.16.1: Patch release that makes cache stampede protection actually fire for cached routine responses. The cache-options documentation has…","frontmatter":{"description":"NpgsqlRest v3.16.1: Patch release that makes cache stampede protection actually fire for cached routine responses. The cache-options documentation has…","head":[["link",{"rel":"canonical","href":"https://npgsqlrest.github.io/guide/changelog/v3.16.1.html"}],["meta",{"property":"og:url","content":"https://npgsqlrest.github.io/guide/changelog/v3.16.1.html"}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:title","content":"Changelog v3.16.1 (2026-06-01)"}],["meta",{"property":"og:description","content":"NpgsqlRest v3.16.1: Patch release that makes cache stampede protection actually fire for cached routine responses. The cache-options documentation has…"}],["meta",{"property":"og:image","content":"https://npgsqlrest.github.io/og-image.png"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:title","content":"Changelog v3.16.1 (2026-06-01)"}],["meta",{"name":"twitter:description","content":"NpgsqlRest v3.16.1: Patch release that makes cache stampede protection actually fire for cached routine responses. The cache-options documentation has…"}],["meta",{"name":"twitter:image","content":"https://npgsqlrest.github.io/og-image.png"}]]},"headers":[],"relativePath":"guide/changelog/v3.16.1.md","filePath":"guide/changelog/v3.16.1.md","lastUpdated":1782480183000}'),n={name:"guide/changelog/v3.16.1.md"};function o(r,e,c,l,h,d){return s(),a("div",null,e[0]||(e[0]=[i(`<h1 id="changelog-v3-16-1-2026-06-01" tabindex="-1">Changelog v3.16.1 (2026-06-01) <a class="header-anchor" href="#changelog-v3-16-1-2026-06-01" aria-label="Permalink to &quot;Changelog v3.16.1 (2026-06-01)&quot;">​</a></h1><h2 id="version-3-16-1-2026-06-01" tabindex="-1">Version <a href="https://github.com/NpgsqlRest/NpgsqlRest/tree/3.16.1" target="_blank" rel="noreferrer">3.16.1</a> (2026-06-01) <a class="header-anchor" href="#version-3-16-1-2026-06-01" aria-label="Permalink to &quot;Version [3.16.1](https://github.com/NpgsqlRest/NpgsqlRest/tree/3.16.1) (2026-06-01)&quot;">​</a></h2><p><a href="https://github.com/NpgsqlRest/NpgsqlRest/compare/3.16.0...3.16.1" target="_blank" rel="noreferrer">Full Changelog</a></p><p>Patch release that makes <strong>cache stampede protection actually fire for cached routine responses</strong>. The cache-options documentation has advertised stampede protection as a HybridCache feature, but the integration used <code>IRoutineCache</code> as a synchronous probe (<code>Get</code> / <code>AddOrUpdate</code>) that could not carry the SQL execution as the cache factory — so the protection never engaged. A burst of identical concurrent requests against a cold cache executed the underlying query <em>N</em> times, each taking a connection. In the worst case this exhausted Postgres&#39; connection pool (<code>remaining connection slots are reserved for roles with the SUPERUSER attribute</code>), which combined with connection-retry backoff could pin the pool long enough to affect every app sharing the database.</p><h2 id="what-changed" tabindex="-1">What changed <a class="header-anchor" href="#what-changed" aria-label="Permalink to &quot;What changed&quot;">​</a></h2><h3 id="iroutinecache-gains-getorcreateasync-additive" tabindex="-1"><code>IRoutineCache</code> gains <code>GetOrCreateAsync</code> (additive) <a class="header-anchor" href="#iroutinecache-gains-getorcreateasync-additive" aria-label="Permalink to &quot;\`IRoutineCache\` gains \`GetOrCreateAsync\` (additive)&quot;">​</a></h3><p>A new method routes the cold-cache work <em>through</em> the cache so concurrent callers for the same key coalesce into a single execution:</p><details class="code-collapsible" open><summary>csharp</summary><div class="language-csharp vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">csharp</span><pre class="shiki shiki-themes catppuccin-latte catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;">ValueTask</span><span style="--shiki-light:#04A5E5;--shiki-dark:#89DCEB;">&lt;</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">object</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">?</span><span style="--shiki-light:#04A5E5;--shiki-dark:#89DCEB;">&gt;</span><span style="--shiki-light:#1E66F5;--shiki-light-font-style:italic;--shiki-dark:#89B4FA;--shiki-dark-font-style:italic;"> GetOrCreateAsync</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">(</span></span>
<span class="line"><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;">    RoutineEndpoint</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> endpoint</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">,</span></span>
<span class="line"><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">    string</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> key</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">,</span></span>
<span class="line"><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;">    Func</span><span style="--shiki-light:#04A5E5;--shiki-dark:#89DCEB;">&lt;</span><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;">CancellationToken</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">,</span><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;"> ValueTask</span><span style="--shiki-light:#04A5E5;--shiki-dark:#89DCEB;">&lt;</span><span style="--shiki-light:#8839EF;--shiki-dark:#CBA6F7;">object</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">?</span><span style="--shiki-light:#04A5E5;--shiki-dark:#89DCEB;">&gt;&gt;</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> factory</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">,</span></span>
<span class="line"><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;">    TimeSpan</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">?</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> overrideExpiration </span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;">=</span><span style="--shiki-light:#D20F39;--shiki-dark:#F38BA8;"> null</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">,</span></span>
<span class="line"><span style="--shiki-light:#DF8E1D;--shiki-light-font-style:italic;--shiki-dark:#F9E2AF;--shiki-dark-font-style:italic;">    CancellationToken</span><span style="--shiki-light:#4C4F69;--shiki-dark:#CDD6F4;"> cancellationToken </span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;">=</span><span style="--shiki-light:#179299;--shiki-dark:#94E2D5;"> default</span><span style="--shiki-light:#7C7F93;--shiki-dark:#9399B2;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div></details><p>It ships as a <strong>default interface method</strong> (plain probe → factory → store, no coalescing), so any pre-existing custom <code>IRoutineCache</code> implementation compiles and behaves exactly as before — it simply gains no stampede protection until it overrides the method.</p><blockquote><p>Note: although this is a new public API surface (conventionally a minor bump), it is shipped as a patch because it fixes an advertised-but-broken feature and is fully backward compatible via the default implementation.</p></blockquote><h3 id="stampede-protection-per-backend" tabindex="-1">Stampede protection per backend <a class="header-anchor" href="#stampede-protection-per-backend" aria-label="Permalink to &quot;Stampede protection per backend&quot;">​</a></h3><ul><li><strong>Memory (<code>RoutineCache</code>)</strong> and <strong>Redis (<code>RedisCache</code>)</strong> — coalesce concurrent factory invocations through an in-flight <code>ConcurrentDictionary&lt;string, Lazy&lt;Task&gt;&gt;</code>. A burst collapses to one execution; the rest await the in-flight result.</li><li><strong>HybridCache (<code>HybridCacheWrapper</code>)</strong> — delegates straight to <code>HybridCache.GetOrCreateAsync</code>, so Microsoft&#39;s built-in stampede protection now genuinely engages.</li></ul><h3 id="middleware-paths" tabindex="-1">Middleware paths <a class="header-anchor" href="#middleware-paths" aria-label="Permalink to &quot;Middleware paths&quot;">​</a></h3><ul><li><strong>Scalar</strong> single-value and <strong>passthrough proxy</strong> responses (value-shaped) route through <code>GetOrCreateAsync</code>. The connection is opened <em>inside</em> the factory, so coalesced waiters never touch the database. The passthrough proxy case additionally coalesces identical upstream HTTP calls.</li><li><strong>Records / sets</strong> (the streaming path) use a <strong>per-key execution gate</strong> instead. This path streams rows to the client and disables caching mid-stream once a response exceeds <code>MaxCacheableRows</code> (default 1000), which does not fit the &quot;compute one value, cache it, share it&quot; factory model. The gate serializes concurrent requests for a key: the lead executes and (within the row limit) populates the cache, so the rest get a cache hit instead of re-executing. This caps concurrent DB executions per key at <strong>one</strong> in all cases — including over-limit responses, which serialize rather than run in parallel.</li></ul><h2 id="effect" tabindex="-1">Effect <a class="header-anchor" href="#effect" aria-label="Permalink to &quot;Effect&quot;">​</a></h2><p>A burst of <em>N</em> identical requests against a cold cache now results in <strong>one</strong> database execution (within-limit) or a single serialized execution at a time (over-limit), instead of <em>N</em> concurrent executions. The worst-case fan-out from one event is bounded by the number of distinct cache keys (bounded by the schema), not by the number of clients.</p><h2 id="test-coverage-read-this-honestly" tabindex="-1">Test coverage (read this honestly) <a class="header-anchor" href="#test-coverage-read-this-honestly" aria-label="Permalink to &quot;Test coverage (read this honestly)&quot;">​</a></h2><p>Automated coverage (<code>NpgsqlRestTests/RoutineCacheTests/CacheStampedeTests.cs</code>) runs against the <strong>in-memory</strong> backend with a live Postgres and asserts execution counts directly:</p><ul><li>50 concurrent cold scalar requests → exactly 1 execution; warm-cache burst → 0 further executions; 4 distinct keys → exactly 4 executions (one per key).</li><li>50 concurrent cold set requests (within limit) → exactly 1 execution; over-limit set (1001 rows) → one execution per request, never cached, all responses correct.</li></ul><p>The <strong>HybridCache</strong> path relies on Microsoft&#39;s own tested coalescing (<code>Microsoft.Extensions.Caching.Hybrid</code>) and the <strong>Redis</strong> path&#39;s coalescing is verified by inspection — neither is exercised by the test harness, which boots the core library with the default memory cache. Claims about those two backends are not backed by an automated test in this repo.</p><h2 id="known-limitations" tabindex="-1">Known limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known limitations&quot;">​</a></h2><ul><li><strong>Cross-process coalescing is out of scope.</strong> Coalescing is in-process per NpgsqlRest instance; multiple instances each execute once. (HybridCache&#39;s Redis layer still shares the <em>cached</em> value across instances.)</li><li><strong>Over-limit sets serialize, not coalesce.</strong> Responses above <code>MaxCacheableRows</code> (default 1000) are never cached, so the per-key gate makes concurrent requests for such an endpoint run one-at-a-time rather than sharing a result. This is deliberate: it caps both concurrent DB executions <em>and</em> peak memory (only one large set renders per key at a time) — but it does reduce throughput for a cached endpoint that returns more than <code>MaxCacheableRows</code> rows under load. Since such an endpoint is never actually cached, the right fix when this matters is to raise <code>MaxCacheableRows</code> so the result caches and coalesces, or to drop the <code>cached</code> annotation (restoring fully concurrent, uncached execution).</li><li><strong>The records/sets gate is held during the response stream.</strong> Because the gate wraps streaming to the client (not just the DB read), a slow or stalled lead client can delay other clients requesting the <em>same</em> key until it finishes or its request cancels. Waiters honor their own cancellation token, so a waiter that gives up is never stuck. The scalar and proxy paths are unaffected — their coalescing slot covers only the upstream call, not the client write.</li><li><strong><code>CommandCallbackAsync</code> short-circuit under coalescing.</strong> If a user-supplied <code>CommandCallbackAsync</code> short-circuits the response on a cached scalar endpoint, coalesced <em>waiters</em> (not the lead) may observe an empty response. This affects only that specific hook on a cached endpoint.</li><li><strong>Cancellation.</strong> The shared factory runs on the lead caller&#39;s token; if the lead cancels mid-flight, waiters retry (re-probe the cache, or one becomes the new lead). A waiter may rarely observe cancellation if the lead cancels at the exact moment of coalescing. This is a deliberate safety choice — the factory uses the lead&#39;s live connection, so fully detaching the shared work risks using a disposed connection.</li></ul>`,22)]))}const u=t(n,[["render",o]]);export{g as __pageData,u as default};
